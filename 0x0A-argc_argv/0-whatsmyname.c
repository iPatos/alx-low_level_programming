#include "main.h"
#include <stdio.h>

/**
 * main - Prints the name of the program
 * @argc: Nbr of comm line arguments
 * @argv: Array name
 * iPatos
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
